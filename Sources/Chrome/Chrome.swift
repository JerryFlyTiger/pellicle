/// Chrome: AppKit/SwiftUI shell: split views, sidebar, tab strip, mode line, command
/// palette panel, settings (SwiftUI), popovers, accessibility. Depends on: Canvas,
/// Terminal, Editor.
///
/// The UI actor never touches a buffer.
/// (PLAN.md 4.2)
package enum ChromeModule {
    package static let moduleName = "Chrome"
}
