/// Extensions: manifests, package manager, Wasm host, XPC host, native module ABI.
/// Depends on: Lisp, Editor, Platform. (PLAN.md 4.2)
package enum ExtensionsModule {
    package static let moduleName = "Extensions"
}
