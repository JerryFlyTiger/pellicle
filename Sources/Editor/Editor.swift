/// Editor: buffers, windows/frames model, keymaps, command loop, minibuffer, completion
/// protocol, undo, faces, overlays, the redisplay planner (produces DisplaySnapshots for
/// Canvas). Depends on: Text, Lisp. (PLAN.md 4.2)
package enum EditorModule {
    package static let moduleName = "Editor"
}
