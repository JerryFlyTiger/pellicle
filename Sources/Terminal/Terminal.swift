/// Terminal: PTY, VT parser, grid overlay, shell integration, input modes. Depends on:
/// Editor, Platform. (PLAN.md 4.2)
package enum TerminalModule {
    package static let moduleName = "Terminal"
}
