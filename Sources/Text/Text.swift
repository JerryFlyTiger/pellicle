/// Text: rope, anchors, interval tree, display transforms, encodings. Depends on:
/// Platform. Testable in isolation (does not depend on Lisp). (PLAN.md 4.2)
package enum TextModule {
    package static let moduleName = "Text"
}
